#include <stdio.h>
#include <string.h>

/**
 * _strstr - string length
 * @haystack: str
 * @needle: str
 * Return: char *
 */
char *_strstr(char *haystack, char *needle)
{
	return (strstr(haystack, needle));
}
