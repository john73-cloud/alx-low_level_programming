#include <stdio.h>
#include <ctype.h>
#include "main.h"
/**
 * mul - check the code.
 *@a: an integer
 *@b: an integer
 * Return: zero  0 or one 1.
 */

int mul(int a, int b)
{
int r;
r = a * b;
return (r);
}
