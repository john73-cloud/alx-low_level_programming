#include <stdio.h>
/**
 * _strcat - concating two strings
 * @dest: destination
 * @src: source
 * Return: char *
 */
char *_strcat(char *dest, char *src)
{
	char *x = strcat(dest, src);

	return (x);
}
