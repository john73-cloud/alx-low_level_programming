#include <stdio.h>
#include <string.h>
/**
 * _strcmp - concating two strings
 * @s1: destination
 * @s2: source
 * Return: int
 */
int _strcmp(char *s1, char *s2)
{
	int x = strcmp(s1, s2);

	return (x);
}
